# Camera Crash Debug Guide

## Проблема
Экран сканера камеры крашится при старте из-за race condition между:
- Запросом разрешений камеры
- Монтированием компонента
- Анимациями навигации

## Применённые исправления

### 1. InteractionManager для ожидания UI
```typescript
InteractionManager.runAfterInteractions(async () => {
  // Запрос разрешения ПОСЛЕ завершения всех анимаций
  await requestPermission();
});
```

### 2. Задержка 200ms перед запросом разрешения
```typescript
await new Promise(resolve => setTimeout(resolve, 200));
await requestPermission();
```

### 3. Состояние isRequestingPermission
Предотвращает множественные запросы разрешений одновременно.

### 4. Строгие условия монтирования камеры
```typescript
{permission.granted && isFocused && uiIdle && !hasError ? (
  <CameraView ... />
) : (
  <LoadingScreen />
)}
```

## Команды для отладки

### 1. Запуск в production режиме (рекомендуется)
```bash
npm run start-prod
```
Это резко снижает краши в Expo Go.

### 2. Сбор логов при краше

#### PowerShell (Windows):
```powershell
# Создать папку для логов
mkdir -Force logs

# Очистить старые логи
adb logcat -c

# Запустить сбор логов
adb logcat -v time *:E AndroidRuntime:E Camera:E CameraX:E | Tee-Object -FilePath logs\camera_crash.txt
```

#### После краша (в другом окне):
```powershell
Select-String -Path logs\camera_crash.txt -Pattern "FATAL|Exception|Abort" -Context 30,30 | Out-File logs\crash_focused.txt
```

### 3. Просмотр только Camera-специфичных логов
```bash
adb logcat -v time Camera:* CameraX:* ExpoCamera:* *:S
```

## Логи для диагностики

При правильной работе вы должны увидеть последовательность:
```
📱 Scanner component MOUNTED
🎥 Screen focused, waiting for UI to settle...
✅ UI idle
🔐 Auto-requesting camera permission after UI settled...
✅✅✅ Camera onCameraReady callback FIRED! ✅✅✅
```

При краше логи остановятся на:
```
🔐 Auto-requesting camera permission after UI settled...
[CRASH - native error, нет JS стека]
```

## Если всё ещё крашится

### Вариант 1: Отключить автозапрос разрешений
В `app/(tabs)/scanner.tsx` закомментируйте блок в useEffect:
```typescript
// if (permission !== null && !permission.granted && !isRequestingPermission) {
//   await requestPermission();
// }
```

Разрешение будет запрашиваться только по кнопке.

### Вариант 2: Увеличить задержку
Измените `200` на `500` или `1000`:
```typescript
await new Promise(resolve => setTimeout(resolve, 500));
```

### Вариант 3: Тестирование на другом устройстве
Проблема может быть специфична для модели/версии Android.

## Известные проблемы

### Samsung + Android 12+
Иногда требует двойного запроса разрешения из-за бага в системе.

### Xiaomi MIUI
Может требовать ручной активации "Display pop-up windows" в настройках.

### Expo Go 2.x на Android 14
Известны краши при быстром переключении экранов. Используйте production режим.

## Контрольный чек-лист

- [ ] Запущен в `npm run start-prod`
- [ ] Устройство подключено по ADB (проверено `adb devices`)
- [ ] Логи собираются в реальном времени
- [ ] Разрешение камеры НЕ дано заранее (удалите в настройках для теста)
- [ ] Переход на экран сканера происходит ПОСЛЕ полной загрузки приложения

## Структура защиты от крашей

```
1. permission === null -> Загрузка
2. isRequestingPermission -> "Requesting permission..."
3. !permission.granted -> Кнопка "Grant access" (с задержкой 200ms)
4. permission.granted + !isFocused -> Камера не монтируется
5. permission.granted + !uiIdle -> Ждём InteractionManager
6. permission.granted + isFocused + uiIdle -> Камера монтируется
```

Каждый этап изолирован и предотвращает race condition.

